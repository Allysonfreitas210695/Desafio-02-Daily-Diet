import { COLORS } from "./Colors";
import { FONT_SIZE, LINE_HEIGHT } from "./FontSize";
import { FONT_FAMILY } from "./FontFamily";

export const theme = {
    COLORS,
    FONT_SIZE,
    FONT_FAMILY,
    LINE_HEIGHT
};
  
export default theme;